/**
 * Course Controller
 * Handles CRUD for courses, publishing toggles, and metadata.
 */
module.exports = {
    getCourses: (req, res) => { }, // Supports Kanban and List views
    createCourse: (req, res) => { }, // Handles small popup creation
    updateCourse: (req, res) => { }, // Main course configuration
    togglePublish: (req, res) => { }, // Website visibility toggle
    getCourseDetails: (req, res) => { }, // For Course Form page
};
