/**
 * Learner Routes
 * Purpose: API endpoints for learners to access courses and track progress.
 */
const express = require('express');
const router = express.Router();
// const { ... } = require('../controllers/LearnerController');

module.exports = router;
