/**
 * Quiz Viewer (Learner)
 * Features: Single question per page, Progress navigation, Score award.
 */
import React from 'react';

const QuizViewer = () => {
    return (
        <div className="quiz-viewer">
            {/* Intro: Total Questions, Attempts notice */}
            {/* Question Loop: Options selection, Proceed button */}
            {/* Result: Complete Quiz button */}
        </div>
    );
};

export default QuizViewer;
